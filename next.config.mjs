/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/mad_soft_test",
  output: "export",
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
